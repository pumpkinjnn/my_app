<% if @chronology_test_answer.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      This contains <%= pluralize(@chronology_test_answer.errors.count, "error") %>.
    </div>
    <ul>
    <% @chronology_test_answer.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
